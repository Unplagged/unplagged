<?php //var_dump($this->barcodes); ?>
<?php if($this->barcodes && count($this->barcodes) > 0): ?>
  <div class="barcodes-big"> 
    <div class="case-teaser">
      <?php foreach($this->barcodes as $barcode): ?>
        <h2 class="h4"><?php echo $barcode['title']; ?></h2>
        <?php echo $barcode['graphic']; ?>
      <?php endforeach; ?>
    </div>
  </div>
<?php endif; ?>