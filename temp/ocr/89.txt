Benjamin Oertel Dec 1], 20]]
s05 22 720
HW9
HW9-1
Implement the Sequential and Binary Search algorithms. Note that there is a bug in the code,
you may have to debug it to ﬁnd it.
Due to the fact binary search works for sorted arrays only, I compared only sorted arrays for
both, the sequential and the binary search.
The code for sequential search:
private Int32 sequentia1Search(Int32[] sortedNumbers, Int32 val)
{
for(int i=0; i<sortedNumbers.Length; i++) {
if (sortedNumbers[i] == val) {
return i;
}
}
return -1;
}
The code for binary search:
private Int32 binarySearch(Int32[] sortedNumbers, Int32 val)
{
Int32 inf = 0;
Int32 sup = sortedNumbers.Length-1;
whi1e(inf <= sup)
{
Int32 mid = (inf+suP) / 2;
if (sortedNumbers[mid] > val) {
sup = mid-1;
} else if (sortedNumbers[mid] < val) {
inf = mid + 1;
} else {
return mid;
}
}
return -1;
}
1

